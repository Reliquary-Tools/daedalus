{
  "name": "@reliquary/daedalus",
  "private": true,
  "version": "0.1.3",
  "type": "module",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build --no-bundle",
    "build:windows": "tauri build --bundles msi",
    "build:mac": "tauri build --bundles dmg --no-sign",
    "build:mac:signed": "tauri build --bundles dmg",
    "build:mac:universal": "tauri build --target universal-apple-darwin --bundles dmg --no-sign",
    "dev:frontend": "vite --host 127.0.0.1 --port 1420",
    "build:frontend": "tsc --noEmit && vite build",
    "preview": "vite preview --host 127.0.0.1"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0",
    "@tauri-apps/plugin-dialog": "^2.7.1",
    "lucide-solid": "^0.468.0",
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "typescript": "^5.7.0",
    "vite": "^6.0.0",
    "vite-plugin-solid": "^2.11.0"
  }
}
